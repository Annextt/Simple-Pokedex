# Simple-Pokedex
Simple Pokédex project built with HTML, CSS and JavaScript.  Uses the PokéAPI to fetch basic Pokémon data such as name, image and type. Created as a learning project to practice DOM manipulation and API consumption.
